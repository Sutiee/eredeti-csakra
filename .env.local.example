# ==================================
# Eredeti Csakra - Environment Variables
# ==================================
#
# SECURITY NOTES:
# 1. NEVER commit .env.local to version control (it's in .gitignore)
# 2. Use NEXT_PUBLIC_* prefix ONLY for client-side variables
# 3. Keep sensitive keys (SERVICE_ROLE_KEY) server-side only
# 4. Rotate keys regularly in production
#
# Copy this file to .env.local and fill in your values

# ==================================
# Site Configuration
# ==================================
# Used for absolute URLs in metadata, sitemaps, and CORS
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ==================================
# Supabase Configuration
# ==================================
# Get these from: https://app.supabase.com/project/_/settings/api

# PUBLIC: Safe to use in browser - has Row Level Security (RLS)
# This key is exposed to the client and is rate-limited
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# PRIVATE: NEVER use in .env.local - Server Actions/API Routes ONLY
# This key bypasses RLS and should NEVER be exposed to the client
# IMPORTANT: Only use this in server-side API routes or Server Actions
# DO NOT prefix with NEXT_PUBLIC_
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ==================================
# Analytics & Monitoring (Optional)
# ==================================
# Google Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry Error Tracking
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# SENTRY_AUTH_TOKEN=your-sentry-auth-token

# ==================================
# Email Configuration (Optional)
# ==================================
# For sending quiz results via email
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ==================================
# Feature Flags (Optional)
# ==================================
# NEXT_PUBLIC_ENABLE_EMAIL_RESULTS=false
# NEXT_PUBLIC_MAINTENANCE_MODE=false

# ==================================
# Development Only
# ==================================
# Set to 'development' or 'production'
NODE_ENV=development

# ==================================
# VARIABLE NAMING CONVENTION:
# ==================================
# ✅ NEXT_PUBLIC_* = Safe for client-side (browser)
#    - API endpoints, public URLs, feature flags
#    - Exposed in browser bundle, visible in DevTools
#
# ❌ NO PREFIX = Server-side ONLY (Node.js)
#    - API keys, secrets, credentials
#    - NEVER accessible from browser
#
# Example:
# NEXT_PUBLIC_API_URL=https://api.example.com  ✅ Client can see
# API_SECRET_KEY=secret123                     ❌ Server only
